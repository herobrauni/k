---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app beszel-agent
spec:
  interval: 1h
  driftDetection:
    mode: enabled
  chartRef:
    kind: OCIRepository
    name: app-template
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      beszel-agent:
        type: daemonset
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: henrygd/beszel-agent
              tag: latest@sha256:5041507ffaefce9635448994def0047c9656ca5cb71b837e5550710954b1bb54
            envFrom:
              - secretRef:
                  name: beszel-agent-secret
            env:
              - name: LISTEN
                value: &LISTEN 45876
            ports:
              - containerPort: *LISTEN
                hostPort: *LISTEN
        pod:
          hostNetwork: true
